# 自定义轨迹图案目录

把图案文件放到本目录，重启上位机后会**自动出现在**“固定轨迹 → 自定义轨迹”下拉框里，
**文件名即下拉框显示名**，无需改任何代码。

## 支持的文件类型

| 扩展名 | 类型 | 下拉框前缀 | 运动方式 |
|---|---|---|---|
| `.svg` | 矢量线条图 | ✏ | 连续描线（画笔绘图；若开启“激光开启”则连续刻线） |
| `.png/.jpg/.jpeg/.bmp` | 位图 | 🔥 | 激光点阵：按灰度逐点驻点打点 |

> 以 `.` 或 `_` 开头的文件、以及子文件夹都会被忽略，可用来存放不想上架的素材。

## SVG 线条图（卡通人物等）

- 用 Inkscape / Illustrator 画好线条后导出 SVG。
- **强烈建议**在 Inkscape 里全选执行“路径 → 对象转路径”(Object to Path)，必要时再
  “描边转路径”，使导出的 SVG 尽量都是 `<path>`，解析最稳。
- 解析器支持 `<path>`（`M/L/H/V/C/S/Q/T/Z/A`）与 `<line>/<polyline>/<polygon>/<rect>/<circle>/<ellipse>`，
  以及 `transform` 与图层 `<g>`。**不支持**文本、位图、滤镜、渐变（只取几何线条）。
- 图案会**等比缩放并居中**到工作区（约 135×85 mm 安全框），无需关心 SVG 原始尺寸。

## 点阵图片

- 放一张**高对比**的图（卡通 / 照片 / Logo）。**深色像素 = 打点**，浅色 = 跳过。
- 自动转灰度、按网格（默认 2 mm 间距）采样，居中到约 120×80 mm。
- 点间距、驻点停留时间是 `motion/motion_mixin.py` 里的 `HALFTONE_*` 常量；**功率用界面“功率”框**。
- ⚠ 出光受“激光开启”开关与 `LASER ARM` 安全链路控制：**不开开关时只逐点空走、不出光**（安全试跑）。
  真机出光前务必按 `Control.md` 的激光调试清单，先低功率、小图验证驻点出光时序。

## 坐标与尺寸

- 一切坐标都是 UI 坐标（mm），最终经 `ui_to_mcu_xy` 自动转 MCU 坐标下发。
- 超出工作区/关节范围的点会被 `validate_trajectory_points` 拦截并在日志提示，不会盲发。
